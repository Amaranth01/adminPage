{% extends 'base.html.twig' %}

{% block body %}
    {% block sidebar %}
        <div class="sidebar">
            <div id="goMenu">
                Menu principal
            </div>

            <div class="list">

                <p>posts</p>
                <p><a href="/index.php?c=article&a=addArticlePage">Ajouter article</a></p>
                <p>Brouillon</p>
                <p>Planifiés</p>
                <p>Corbeille</p>
            </div>
        </div>
    {% endblock %}

    {% block content %}
        <div class="content">
            {% if articles is defined %}
                {% for article in articles %}
                    <div class="listArticle">
                        <h2>{{ article.article_title | e }}</h2>
                        <p>{{ article.article_content | e }}</p>
                    </div>
                {% endfor %}
            {% endif %}
        </div>
    {% endblock %}

{% endblock %}